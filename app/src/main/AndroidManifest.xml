<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thurainx.taskapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskApp"
        tools:targetApi="31">
        <activity
            android:name=".activities.ProfileActivity"
            android:theme="@style/Theme.TaskApp.translucentScreenWhiteStatusBar"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".activities.CreateTaskActivity"
            android:theme="@style/Theme.TaskApp.translucentScreenWhiteStatusBar"
            android:exported="false" />

        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/Theme.TaskApp.translucentScreenDarkStatusBar"
            android:exported="true">

        </activity>
    </application>

</manifest>